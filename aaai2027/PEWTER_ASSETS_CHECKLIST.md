# PEWTER paper — assets checklist

Every `\ph{}`/`XXXX` marker currently in `aaai2027/pewter_aaai.tex`, plus the plots/figures the
paper needs that aren't marker-driven, one row each. **Status** legend: DONE (real content, no
action needed), BLOCKED-CITATIONS (needs your bibkeys, nothing else to do), NEEDS-FIGURE (prose
is drafted, the actual plot/table graphic doesn't exist yet), NEEDS-DATA (the analysis itself
hasn't been run), OPEN-QUESTION (needs a decision/discussion before it can be written at all).

**2026-08-06 update — paper moved to WSDM format.** Active source is now
`aaai2027/WSDM_format_revised.tex` (the AAAI source `pewter_aaai.tex` is kept for reference/history
only — every row below that cites a location in "the tex" now means the WSDM file unless noted).
Two changes to how this checklist tracks citations going forward:
- **Citation placeholders are gone from the WSDM draft and should not be reintroduced.** Any
  `\citep{PLACEHOLDER: ...}` you see below is a description of what the *old AAAI file* had at
  that spot, kept only so a future pass knows which works were the candidates — it is not
  something to paste back into the WSDM tex. New/rewritten prose in the WSDM file should read as
  clean text with no citation markup at all; you're adding the real `\cite`s yourself.
  BLOCKED-CITATIONS rows below are therefore mostly informational now (what to eventually cite),
  not action items for me.
- **Multi-seed statistics campaign started 2026-08-06** (see row #17): replaces the single-split
  Hanley–McNeil SE with real mean±std AUC across 10 splits (seed 42 + 9 new seeds 43–51), for
  \method\ (both attention variants, all 6 datasets) and, lowest priority, GINEConv. Driver:
  `scripts/run_multiseed_pewter.py`, logs in `logs/multiseed/`. In progress as of this update —
  see row #17 for current status.

**2026-07-21 update:** in-text `\ref`s for tables/figures now enforced as a house style rule
(see the file's top-of-file style-note block); bibliography position fixed (was before
`\appendix`, now correctly after it, per `AuthorKit27/AnonymousSubmission2027.tex`'s required
section order); the `\input{ReproducibilityChecklist.tex}` question is moot — checklist is
submitted separately, no `\input` is present in the file. Five new rows added (#31–#35) for
items raised this round: architecture-schematic prompt, runtime log-log scaling curve, the
ablations composite figure, the GNN-baseline-list decision, and the full citation inventory. Two
items (#18 Algorithm box, #19 Walk sampler) are marked NEEDS-DISCUSSION, not just
NEEDS-FIGURE/DATA — see chat, both need a live back-and-forth before more text gets written, not
another silent rewrite.

## Front matter / framing

| # | Location | What's needed | Status | Source / notes |
|---|---|---|---|---|
| 1 | Abstract — empirical summary line | one-line summary of what Empirical Confirmation shows | DRAFTED, pending figure | ties to #12 |
| 2 | Abstract — AUC gain number | the headline average AUC gain | **UNBLOCKED (2026-07-30 correction)** — `CLAUDE.md`'s SOTA table already reflects the post-retrain `edge_cover` numbers (E25/E26/E27), not pre-retrain `k_cover`; `CLAUDE.md` had a stale, self-contradicting note claiming otherwise, now fixed. Ready to transcribe. | `CLAUDE.md` SOTA table (E25/E26 full-attn, E27 LocalAttn4 columns) |
| 3 | Abstract — "gain is largest where entropy is high" | simplified to one direction-agnostic line | DONE (2026-07-20) | `LEAD4C_DIRECTIONALITY_ANSWERS.md` |
| 4 | Abstract — "attention recovers direction of information flow" | left as an explicit open placeholder | STILL a stub as of 2026-08-04 — #22 now has real findings to draw on, but the abstract sentence itself was not rewritten (flagged, not silently rewritten, per the standing rule) | — |
| 5 | Introduction — "put refs all along" | citations throughout | BLOCKED-CITATIONS | see #35 for the full inventory |
| 5b | Introduction — no-external-features framing (WSDM tex, right before the "classical recipe" paragraph) | placeholder asked for a few sentences on why \method\ doesn't use external vertex/edge features (text, timestamps, account metadata) when some prior work does | **DONE (2026-08-06)** | states we restrict every model in the comparison (ours and baselines) to topology + observed signs only, for two reasons: side features aren't comparably available across the six datasets, and the paper's question is specifically how much label information graph structure alone carries — no citations added (WSDM draft convention, see top-of-file note) |
| 6 | Contributions bullet 2 — forward-ref | `\ref` to Empirical Confirmation figure | NEEDS-FIGURE | blocked on #12 existing with a `\label` |
| 7 | Contributions — "anything interesting on the averaging" | confirm the drafted answer is what was meant | DRAFTED, needs your confirm | — |
| 8 | Related Work — 4 `\citep{}` blocks (balance theory, status theory, SGCN/SiGAT/SNEA/SDGNN, DeepWalk/node2vec, Graphormer/GPS) | real bibkeys — **you're sourcing these yourself now**; see chat for a concrete search-term guide per paragraph, kept independent of the Baselines list (#15) on purpose | BLOCKED-CITATIONS | see #35; **all 4 paragraphs rewritten 2026-07-21** to remove every comparison-to-\method / forward-reference sentence — now pure prior-work description, organized around information flow per the professor's original note (see chat for what that means) |
| 9 | Related Work — "anything else I missed" | judgment call on 3 candidate extra areas (KG relation prediction, oversquashing lit, position-aware GNNs) | DRAFTED candidates, needs your judgment + citations | see chat for what "anything else I missed" and "do not give a list" most likely mean |
| 10 | Related Work, Vertex-centric GNNs paragraph — forward-ref | `\ref` to Empirical Confirmation | NEEDS-FIGURE | same blocker as #6 |
| 11 | Problem Setting — "REFS HERE" (WL-coloring/GNN expressiveness) | citations | BLOCKED-CITATIONS | see #35 |

## Empirical Confirmation section

| # | Location | What's needed | Status | Source / notes |
|---|---|---|---|---|
| 12 | **5-panel figure (2026-08-04 rework, was 3-panel)**: schematic / directed+undirected MI decay / GNN-accuracy-vs-entropy / sign-agreement AUC bars / regression coefficients | ALL FIVE PANELS DONE (2026-08-04), merged into one combined image | **Panel A (2026-08-04, NEW — retires the old entropy-asymmetry boxplot panel):** schematic of the position-index notation (target edge = 0, endpoints = ±1, their other edges = ±2) reused by the new Panel E. Scripts: `scripts/paper_figures/plot_empconf_panelA_schematic.py` (no extract step, static diagram) — see also `scripts/paper_figures/PANELA_SCHEMATIC_GUIDE.md` and the parallel hand-editable `aaai2027/figures/empconf_panelA_schematic.drawio`. **The old entropy-asymmetry boxplot panel is dropped from Figure 1 entirely per the user's explicit call (2026-08-04)** — its own script/data (`{extract,plot}_empconf_panelA_entropy_boxplot.py`, `empconf_panelA_entropy_boxplot.csv`/`_ttest.csv`) still exist on disk (unmodified) but are no longer wired into this figure. **`pewter_aaai.tex`'s `\textbf{(A) Entropy asymmetry.}` prose paragraph (with its own Wilcoxon/meta-analysis numbers) is now ORPHANED — it still references `Figure~\ref{fig:empconf-panels}a`, which is the schematic now, not a boxplot. Not auto-rewritten; needs your call (keep as text-only without a figure ref / cite the stat some other way / drop).** Panel B: real recomputation as before (NOT `outputs/mi_analysis_package.zip`, see #30) — **the post-minimum "bump" at distance 4-6, RESOLVED (2026-07-29/30): a proper block/cluster bootstrap confirms the bump is real at every tested distance on slashdot090221 (only dataset tested); full writeup `PANELB_INVESTIGATION_REPORT.md`.** **2026-08-04, NEW: Panel B now also shows a DIRECTED (out-neighbors-only BFS) curve alongside the existing undirected one, solid vs.\ dashed, same plot** — `extract_empconf_panelB_mi_decay_linegraph.py` gained a `--direction {undirected,directed}` flag (default `undirected`, existing callers unaffected), porting the successor-only adjacency from the one-off diagnostic `scripts/ablation_slashdot_colleague_vs_ours.py` (which only ever covered slashdot090221) into a proper all-6-dataset production run. New data: `aaai2027/figure_data/empconf_panelB_mi_decay_linegraph_directed.csv`; undirected CSV unchanged/reused. **The tex `%%` comment above the old Panel B paragraph (superseded degree-confound framing) is still stale — see #12b, unrelated to this rework, still open.** Panel C: unchanged, reused as-is (GINEConv + SiGAT — **raw SiGAT, not SGA-augmented, corrected 2026-08-04, see #21/#34**), scripts `{extract,plot}_empconf_panelC_gnn_entropy_heatmap.py` — the plot script's own `DISPLAY_LABEL` dict had the same "SiGAT (SGA)" mislabel bug, fixed in the same pass. **Panel D (2026-08-04, NEW):** 4-way sign-agreement AUC bars (in-same/in-diff/out-same/out-diff, per Panel A's u/v notation) for GINEConv + SiGAT, pooled across 6 datasets, reusing the same shared-edge canonical predictions as Panels C/E (`outputs/lead4_entropy_heterogeneity/predictions_raw_canonical.pkl`). One prediction instance contributed per qualifying neighbor-edge pair (confirmed with the user — an edge can land in multiple buckets, counted multiple times within one). Scripts: `scripts/paper_figures/{extract,plot}_empconf_panelD_signagreement_auc.py`. Data: `aaai2027/figure_data/empconf_panelD_signagreement_auc.csv`. Finding: both baselines' AUC is markedly lower on `*_diff` buckets (disagreeing neighbor sign) than `*_same` buckets. **Panel E (2026-08-04, NEW):** pooled, $z$-scored logistic-regression coefficients (`node4_zscored` spec, GINEConv + SiGAT only — not the walk model) from `outputs/lead4c_srctgt_export.zip`'s `results/fit_results_all6.csv`, relabeled `src_out/src_in/tgt_out/tgt_in` → `H_out(-1)/H_in(-1)/H_out(1)/H_in(1)` to match Panel A. Raw beta (not negated, per user's call). Scripts: `scripts/paper_figures/{extract,plot}_empconf_panelE_coefficients.py`. Data: `aaai2027/figure_data/empconf_panelE_coefficients.csv`. Combine: `scripts/paper_figures/combine_empconf_panels_abcde.py` (supersedes `combine_empconf_panels_abc.py`), output `aaai2027/figures/empconf_panels_abcde_combined.png`, layout row1=A+B, row2=C, row3=D+E. |
| 12b | Panel B `.tex` inline `%%` comment (lines ~216-232 of `pewter_aaai.tex`, right above the Panel B paragraph) | still describes the superseded degree-confound/"fix not yet implemented" framing from 2026-07-28, predates the cluster-bootstrap resolution (#12) | OPEN-QUESTION, LOW EFFORT ONCE DECIDED | Needs a decision, not just an edit: whether to mention the clustering/cluster-bootstrap finding in the paper's visible prose at all (currently the visible caption/prose only states the MI-collapse magnitude, not the bump or its mechanism), vs. just refresh the internal `%%` dev-comment to point at `PANELB_INVESTIGATION_REPORT.md` and leave prose untouched. Either way the comment's stale content should be updated so a future session doesn't rediscover it as if unresolved. |

## Methods (mostly done)

| # | Location | What's needed | Status | Source / notes |
|---|---|---|---|---|
| 13 | Aggregation footnote | alt-weighting comparison numbers | DONE | `POSTHOC_RESULTS_ALL_DATASETS.md` §6.5 |
| 14 | Complexity — why $\kappa$ varies 1–5$\times$ by dataset | characterize the scaling law | NEEDS-DATA, genuine future work, not blocking | — |
| 15 | Baselines — final list + citations | list settled (2026-07-21): GINEConv + SiGAT, SNEA, CSG/CSG-GSGNN, GSGNN+SGA, CopulaLSP. **SE-SGformer fully dropped, not even mentioned** (corrected 2026-07-21 — an earlier pass mentioned-and-excluded it with a caveat, which was wrong; you said ignore it, so it's just not there) | DONE (list), BLOCKED-CITATIONS (bibkeys) | see #34; Related Work paragraph rewritten (see #8) |
| 16 | Setup — bitcoin-alpha/otc plateau caveat | honest caveat already written | DONE | `CLAUDE.md` Walk sampler table |
| 17 | Statistical evaluation — AUC + standard error | **SUPERSEDED 2026-08-06**: single-split Hanley–McNeil SE being replaced with real mean±std across 10 splits | **IN PROGRESS** — bitcoin-alpha done (both attention variants, all 11 aggregator functions); wiki-elec running; bitcoin-otc/wiki-rfa/epinions/slashdot090221 queued; GINEConv (lowest priority) queued last | Driver `scripts/run_multiseed_pewter.py`, 10 seeds (42 reused + 43–51 new) per (dataset, attention variant), all 11 `run_posthoc.py` aggregator functions computed per seed so Ablation B doesn't need a second pass. GINEConv needed its own fix: its `--seed` flag only touches model init, not the data split, and its canonical split (`baselines/splits_canonical/`) only existed for seed 42 — `baselines/prepare_splits.py::save_canonical_split_for_seed()` now builds a genuine per-seed canonical split from that seed's own walk cache before each GINEConv run. Old single-split numbers (this row's previous "DONE 2026-07-26" state, `scripts/paper_figures/hanley_mcneil.py` + `test_set_counts.csv`) still stand until this campaign finishes and Table 1 / Ablation A / Ablation B are rebuilt off it. |
| 18 | Algorithm boxes | one box only: Algorithm~1 (`alg:pewter`, inference for edge $(u,v)$, scope stated up front) | DONE (2026-07-22) | a second box for corpus construction was drafted 2026-07-21 then removed 2026-07-22 (user call) -- the two-phase sampling loop is generic enough that the Walk sampler paragraph's prose already says everything the pseudocode did; `alg:pewter`'s Input line updated to point at that paragraph instead of the removed box |
| 19 | Walk sampler paragraph | rewritten to describe the real two-phase mechanism (anchor pass guaranteeing coverage + dedup-filtered fill pass), re-verified against `src/data/coverage_aware_sampler.py::edge_cover_walks` | DONE (2026-07-21) | root cause of prior badness: earlier drafts described the corpus as if uniformly sampled throughout, which is only true of the fill phase — the anchor phase is what actually provides the coverage guarantee, and that distinction was missing from the prose every prior pass |

## Results / Ablations — the bulk of the remaining figure work

| # | Location | What's needed | Status | Source / notes |
|---|---|---|---|---|
| 20 | Result 1 — overall AUC | expanded table (was a 4-column "best GNN" summary, now every individual baseline we have a canonical-split number for: CSG, CSG-GSGNN, GSGNN+SGA, SNEA, CopulaLSP, GINEConv, SiGAT, plus blank GCN/GAT placeholder rows) + AUC±SE per cell | DONE (2026-07-26) as a sketch; retrain blocker **claim from 2026-07-30 was itself WRONG, corrected 2026-08-04** — see below | `baselines/all_results_canonical.csv` + `baselines/SGA/results/result_GSGNN_<ds>_canon_1_SGA.csv` (GSGNN+SGA, only 3/6 datasets) + `CLAUDE.md` Attention-variant table (Pewter rows) + Hanley-McNeil SE (#17). GCN/GAT rows intentionally blank, not fabricated. **2026-07-27: reverted back to single-column `table`** per your call (was briefly `table*`/full-width) -- will render dense/small given 7 data columns, but matches your preference; revisit if the compiled PDF shows it's actually illegible. **STALE, corrected 2026-08-04**: the 2026-07-30 note above claiming "no data change needed" was checked against `CLAUDE.md`'s table *as it stood 2026-07-30* (E27 LocalAttn4) — `CLAUDE.md`'s table was then updated 2026-08-02 to the post-Python-migration `E31_PY314_MIGRATION` retrain, and this table's `\method (LocalAttn4)` row (0.913/0.939/0.953/0.906/0.896/0.898) still exactly matches the *old* E27 numbers, not current E31 (0.9215/0.9356/0.9528/0.9045/0.8938/0.9002). Full `\ph{PLACEHOLDER}` flag added to the table caption in the tex. E31 checkpoints exist on disk for all 6 datasets (`outputs/<ds>/E31_PY314_MIGRATION_*`) so a rebuild (new AUC±SE from real E31 predictions) is possible without retraining — not done, needs sign-off given this is the headline table. **Bonus catch same session:** the SiGAT row was also labeled `SiGAT (SGA)`, a stale mislabel predating the raw-SiGAT correction (#21/#29/#34) — fixed to plain `SiGAT`. |
| 21 | Result 2 — entropy sensitivity | **REMOVED 2026-08-05 (user call)**: figure dropped from the paper, prose kept | **FIGURE PULLED, text stands as a placeholder pending a redesign** | Rebuilt 2026-08-04 against `E31_PY314_MIGRATION` (full attn)/`E32_PY314_LOCALATTN4` (local attn), all 6 datasets, SiGAT (raw, not SGA-augmented) + GINEConv — that rebuild is real and verified, kept in `aaai2027/figure_data/result2_entropy_heatmap.csv` and `aaai2027/figures/result2_entropy_heatmap.png` (both still on disk, just not referenced from the tex anymore). **2026-08-05: the `\begin{figure}...\end{figure}` block and its `\label{fig:result2-heatmap}` were removed from `pewter_aaai.tex`** (was rendering too small/cramped as a single-column figure, and the presentation may change — e.g. discrete 4x4 bins matching Empirical Confirmation Panel C instead of the kernel-smoothed surface). The body paragraph was kept, rewritten to describe the finding in prose without a `Figure~\ref` (no dangling reference), with a `\ph{PLACEHOLDER}` marking that a figure will be re-added once the presentation is decided. Scripts/data unchanged and ready to reuse for whatever the next version looks like: `scripts/paper_figures/{extract,plot}_result2_entropy_heatmap.py`, walk data via `extract_result2_walk_entropy_fresh.py`. |
| 22 | Result 3 — attention and direction of information flow | the whole analysis | **DONE (2026-08-04)**, extended same day with a 4th panel; **whole figure flagged STALE same day (checkpoint provenance audit)** | Design: for each masked target-edge token, signed offset `d=j-i` split into forward (`d>0`, toward `v`) / backward (`d<0`, toward `u`), and node/edge token role, on \method\ (local-attention checkpoint), layer 0, all 6 datasets — `scripts/attention_directionality.py` (inference, all layers/heads, cached to `outputs/attention_directionality/*_result.pkl`) → `scripts/paper_figures/extract_attndir_panelA_headgrid.py` + `extract_attndir_panelBC_summary.py` (reslice/aggregate, no new inference) → `plot_attndir_panel{A,B,C}_*.py` → `combine_attndir_panels.py`. **Panel D added same day**: PEWTER's own binned entropy-vs-AUC heatmap (`extract_attndir_panelD_pewter_entropy_heatmap.py` reuses `computed_data.pkl`'s `walk_localattn4` model, same `out_in` variant/binning as Empirical Confirmation Panel C) → `plot_attndir_panelD_pewter_entropy_heatmap.py`, folded into `combine_attndir_panels.py`. Data: `aaai2027/figure_data/attndir_panelA_headgrid.csv`, `attndir_panelBC_summary.csv`, `attndir_panelD_pewter_entropy_heatmap.csv`. Figure: `aaai2027/figures/attndir_panels_abcd_combined.png`, in the paper as `fig:attndir` (single-column `figure`). **Naming convention (2026-08-04): this whole figure says "PEWTER" not "LocalAttn4" in every plot title/label** — it never compares against the full-attention variant, so per the user's call the full/local distinction isn't surfaced here (contrast with Result 1's table, Result 2's heatmap, and Ablation A, which all keep "LocalAttn4" explicitly since they *are* full-vs-local comparisons). ~~Finding (E27-era, SUPERSEDED 2026-08-05): forward/backward mass close to balanced on 4/6, backward-dominant only on wiki-elec/wiki-rfa~~ — **see the REBUILT note at the end of this row for the corrected finding on the real post-migration checkpoint.** Node/edge split shows no comparably clean pattern (this part held up on rebuild). Panel D's PEWTER heatmap is directly comparable to Empirical Confirmation Panel C's SiGAT-only heatmap (same shared-edge data, same binning). Semantics caveat ("does attention weight = information flow") kept as an explicit `\ph{PLACEHOLDER}` in the tex, not resolved, plus a note that this is layer-0/local-attention-only scope, not yet extended to other layers or full attention. **Bonus catch while touching this area:** Result 1's SOTA table had a stale `SiGAT (SGA)` row label (line ~337) predating the 2026-08-04 raw-SiGAT correction (#21/#29/#34) — fixed to plain `SiGAT`. **STALE, flagged 2026-08-04 (same-day provenance audit, triggered by the user asking whether recent walk-model figures/tables reflect the E31 retrain):** panels (a)-(c) run on `E27_NOHARD_EDGECOVER_LOCALATTN4` (`attention_directionality.py`'s `LOCAL_RUN_INFO`), pre-migration and superseded by `E31_PY314_MIGRATION`. Panel (d) is worse — `computed_data.pkl` (mtime 2026-06-29) predates even E27 (tagged `20260719`); confirmed via `plot_ablationA_full_vs_local.py`'s own comment that this pickle is built from a pre-`edge_cover`-sampler checkpoint, i.e. older than E25/E26/E27 entirely, not just pre-migration. A parallel, E27-based file does exist (`predictions_raw_canonical_e27.pkl`, from `baselines/postprocess_canonical_e27.py`) but nothing E31-based has been built yet at any level (no `_e31` predictions pkl, no E31 rerun of `attention_directionality.py`). `\ph{PLACEHOLDER}` added to the figure caption in the tex. E31 checkpoints exist on disk for all 6 datasets (`outputs/<ds>/E31_PY314_MIGRATION_*`), so a full rebuild is possible without retraining — not done, needs sign-off (this is the newest figure in the paper and would need to be regenerated close to end-to-end: new `attention_directionality.py` run for panels a-c, a new `_e31` canonical-predictions pkl for panel d). **REBUILT 2026-08-05**: full end-to-end rebuild done, all 6 datasets, on the *real* corrected checkpoints — `E31_PY314_MIGRATION` turned out to be full attention (not LocalAttn4, see `CLAUDE.md` "Current SOTA"), so panels (a)-(d) actually rebuild against `E32_PY314_LOCALATTN4` (the properly-configured LocalAttn4 retrain) instead. New pipeline: `baselines/postprocess_canonical_e32.py` (new script, modeled on `_e27.py`) → `outputs/lead4_entropy_heterogeneity/predictions_raw_canonical_e32.pkl` → `lead4_entropy_heterogeneity.py --mode compute --predictions ..._e32.pkl --out outputs/lead4_entropy_heterogeneity_e32/` → panel (d) extract now reads `outputs/lead4_entropy_heterogeneity_e32/computed_data.pkl` (not the old stale `computed_data.pkl`, which stays untouched for anything else that still points at it). **Corrected finding (layer 0, all 6 datasets, real E32 checkpoint): the "wiki-only flip" does NOT survive.** bitcoin-alpha and slashdot090221 are forward-dominant (0.443/0.411, 0.417/0.363); bitcoin-otc, epinions, wiki-elec, AND wiki-rfa are all backward-dominant (0.403/0.445, 0.376/0.452, 0.311/0.432, 0.189/0.557) — a 2-vs-4 split, not 4-balanced-vs-2-flipped. wiki-rfa is still the most extreme case, but backward-dominance is now the *majority* pattern, not a wiki-specific anomaly. This is flagged prominently in the tex (`\ph{PLACEHOLDER}` in the Result 3 body) as a genuine finding change from re-checking the checkpoint, not quietly re-fit to the old narrative. **Panel D dropped 2026-08-05 (user call)**: the figure is back to 3 panels (a)-(c), `combine_attndir_panels.py` updated to a 2-row layout and now writes `attndir_panels_abc_combined.png` (the old `_abcd_combined.png` name is retired). `extract_attndir_panelD_pewter_entropy_heatmap.py`/`plot_attndir_panelD_pewter_entropy_heatmap.py` kept on disk, unused. |
| 23 | Ablation A — full vs. local attention ("proximal attention is enough") | AUC comparison, now a table | **REBUILT 2026-08-04 as `tab:ablationA`, converted from bar chart to table** (per your call — "the whole ablations are probably tables now not figs") | Full attn = `E31_PY314_MIGRATION` (adopted post-migration retrain), LocalAttn4 = `E32_PY314_LOCALATTN4` (post-migration, 4/5 datasets in hand — LocalAttn4 matches/beats full attn on bitcoin-otc/epinions/wiki-elec/wiki-rfa, trails on bitcoin-alpha; slashdot090221 pending, its E32 training still in progress). Same underlying numbers as Table 1 (`tab:result1`), reused via `scripts/paper_figures/extract_result1_walk_aucs.py` / `aaai2027/figure_data/result1_walk_aucs.csv` — no separate extraction needed. The old `plot_ablationA_full_vs_local.py` bar-chart script and `ablationA_full_vs_local.csv`/`.png` are now unused by the paper (kept on disk, not deleted). Fill in slashdot090221's row once its E32 posthoc completes. |
| 24 | Ablation B — smart sampling | ~~coverage comparison figure~~ | **DROPPED 2026-07-26** | your call: `edge_cover`'s anchor-then-uniform-fill mechanism guarantees coverage for fair evaluation, it isn't a targeted/adaptive "smart" sampling strategy, so it doesn't carry as a performance ablation. Paper's old B) paragraph replaced with a dev-comment explaining the drop; revisit only if the advisor asks for it in some other form. |
| 25 | Ablation B (paper letter) — weighting/averaging (posthoc aggregation) | curated functional-aggregator sweep, now a table | **REBUILT 2026-08-04 as `tab:ablationB`, converted from bar chart to table, candidate set curated** (per your call); **relettered end-to-end 2026-08-05** | **Relettered to "Ablation B" in the paper's visible prose/caption 2026-08-05** (the old "Ablation B" "smart sampling" was dropped, #24, leaving a visible A→C gap in the rendered text). **Fully relettered in code/data too, not just the paper text**: `\label{tab:ablationB}` (was `tab:ablationC`), `extract_ablationB_full_sweep.py`/`plot_ablationB_full_sweep.py`/`combine_ablations_ab.py`/`ablationB_paired_significance.py` (all `git mv`'d from their `ablationC_*`/`_ac.py` names), `aaai2027/figure_data/ablationB_full_sweep.csv`/`ablationB_summary3.csv`, `aaai2027/figures/ablationB_full_sweep.png`/`ablations_ab_combined.png`, and the on-disk posthoc run directories `outputs/<ds>/E32_PY314_LOCALATTN4_*/posthoc/ablationB_e32/` (renamed from `ablationC_e32/`, all 6 datasets) — the extract script's `RUN_ID` was updated to match and rerun to confirm it still finds the renamed directories (numbers unchanged, exact match). Checkpoint: `E32_PY314_LOCALATTN4`, all 6 datasets complete. **Candidate set narrowed from ~39 to 11**: only functions depending on `q` alone (no position/length), one-sided `(1-q)`-only forms dropped entirely (not just deprioritized) — see #22's row for the full kept/dropped list. **Two new functions added to `run_posthoc.py::_func_registry`**: `func_fisher_power` (`(q(1-q))^{-b}`, Fisher-information/inverse-variance weighting — ties directly to the paper's existing "comparable to inverse-variance weighting" Methods language) and `func_maxprob_power` (`max(q,1-q)^b`, confidence-in-predicted-class). Finding: full spread under 0.002 AUC across all 11 functions/6 datasets; production choice (`func_logit_power`) best-or-tied on 2/6; the Fisher-info function is the single best performer on epinions (0.9537) and slashdot090221 (0.8983) — a genuine, if small, signal. |
| 26 | Conclusion — headline number sentence | final numbers once Results settle | PENDING, but #20's retrain blocker is now lifted (2026-07-30) and #22 is now DONE (2026-08-04) — no remaining dependency, just not yet written | depends on #20–22 |
| 27 | Bibliography | real bibkeys | BLOCKED-CITATIONS | see #35; **position now fixed** (2026-07-21) — was before `\appendix`, now correctly after it and right before `\end{document}`, matching `AuthorKit27/AnonymousSubmission2027.tex`'s required section order |

## Plots not tied to a specific marker

| # | Plot | Status | Notes |
|---|---|---|---|
| 28 | **Architecture schematic** | you're building it directly (`aaai2027/figures/pipeline_schematic.drawio`) | REFERENCED in paper 2026-07-26, PNG export pending; single-column `figure` (reverted 2026-07-27, was briefly `figure*`) | Placed right after the "\method\ has three parts" sentence at the top of Methods (your call). `figure*` (full-width) with a caption describing the 2-stage pipeline (Transformer trained+frozen, then a separately-trained aggregator), `\label{fig:pipeline-schematic}`. **Blocked only on the PNG export** — `\includegraphics{figures/pipeline_schematic.png}` points at a file that doesn't exist on disk yet; drop the exported PNG at that exact path and the reference already works, no further `.tex` changes needed. |
| 29 | GNN AUC vs. entropy heatmap (Panel C) | DONE, **REVERTED 2026-07-27 back to GNN-only (2 rows)** | Briefly had a 3rd \method\ row (2026-07-26) merging this with Result 2; **your call 2026-07-27: keep Panel C as the original 2-row GNN-only comparison** (SiGAT, raw — **not** SGA-augmented, corrected 2026-08-04, see #21 — + GINEConv), and give Result 2 its own separate 4-row figure instead (#21), even though that duplicates these same 2 GNN rows. Kernel-smoothed (Gaussian-weighted AUC, no grey/empty cells) over `out_in` entropy axes, 2-row×6-col layout. Scripts: `scripts/paper_figures/{extract,plot}_empconf_panelC_gnn_entropy_heatmap.py`. Data: `aaai2027/figure_data/empconf_panelC_gnn_entropy_heatmap.csv`. `extract_walk_entropy_fresh.py` (the LocalAttn4-only fresh-walk extraction built for the brief merge) is now unused by Panel C but still consumed by Result 2's own extraction (#21). **2026-08-04: Panel C is being reworked into a 5-panel Figure 1 (A schematic, B directed-NMI, C this heatmap reused as-is, D sign-agreement bars, E regression coefficients) — see chat/plan, execution in progress.** |
| 30 | MI-decay-with-distance curve | DONE (2026-07-24) | Real recomputation, not `outputs/mi_analysis_package.zip` (whose BFS-distance metric this replaces, sidestepping that script's bug entirely rather than waiting on a fix): new **line-graph (edge-adjacency) distance** metric, architecture-agnostic (checked GINEConv's and SiGAT's actual real aggregation schemes first, neither matches a simple directed-hop distance — see `aaai2027/edge_distance_mutual_information_notes.md`). Linear y-axis (not log — log was visually inflating small absolute fluctuations into what looked like a dramatic reversal; verified via a sign-shuffle control that the small residual on slashdot/wiki-rfa at distance 5-6 is real, but per user call the prose/caption stay purely descriptive, no discussion of that residual). Scripts: `scripts/paper_figures/{extract,plot}_empconf_panelB_mi_decay_linegraph.py` (extract script also supports `--shuffle-signs` for the control test). Data: `aaai2027/figure_data/empconf_panelB_mi_decay_linegraph.csv`. Feeds Empirical Confirmation panel B (#12). |
| 30b | **Panels B+C combined into one figure** | DONE (2026-07-24), converted to `figure*` 2026-07-26 | Two separate `figure` floats let the heatmap drift away from its paragraph on the page; merged into a single image (`scripts/paper_figures/combine_empconf_panels_bc.py`, stacks the two panel PNGs vertically with (a)/(b) labels) so LaTeX treats them as one float. **2026-07-26: briefly converted to `\begin{figure*}` (full-width) over a legibility concern (paper is `\twocolumn`, single-column is ~3.3in); reverted 2026-07-27 per your call back to single-column `figure`, matching how it looked/fit before.** Output: `aaai2027/figures/empconf_panels_bc_combined.png`, in the paper as `fig:empconf-panelbc` (referenced as `\ref{fig:empconf-panelbc}a` / `b` from paragraphs B and C respectively). Re-run the combine script after re-plotting either panel. |

## New this round (2026-07-21)

| # | Item | Status | Notes |
|---|---|---|---|
| 31 | **Architecture schematic — concrete plan** | NOT STARTED | Proposal: write a detailed prompt (component list + data flow + labels) for a draw.io/diagram-generation agent session, rather than hand-drawing it in this session. Needs: what to show (sampler → per-edge walk corpus → tokenized sequence → transformer encoder w/ masked-edge objective → per-walk score → weighted-vote aggregation), and how much internal detail (e.g., show the local-attention window explicitly?). Spin out once you confirm the component list. |
| 32 | **Runtime log-log scaling curve** | NOT STARTED, NEEDS-DATA (new experiment) | Modeled on the HELM-paper example you pointed to: wall-clock (sampling + train/inference) vs. graph size on a log-log plot. Proposed design: synthetic graphs with increasing $\lvert E\rvert$ (or total walk budget $M$), holding $L$/$d$/etc. fixed, to empirically confirm the $O(\lvert E\rvert)$ total-sampling-cost claim from Complexity (#14). No such sweep exists yet — this is genuinely new work, not a transcription job; needs sign-off before running (matches the "new experiments only as flagged TODOs, not run silently" rule). |
| 33 | ~~Ablations composite figure~~ | **RETIRED 2026-08-04** — both panels are now separate tables, not a combined figure | Superseded by #23/#25's table conversion (`tab:ablationA`, `tab:ablationB`) — no combined image needed anymore. `combine_ablations_ab.py` (renamed 2026-08-05 from `combine_ablations_ac.py`) and `ablations_ab_combined.png` (renamed from `ablations_ac_combined.png`) are now unused (kept on disk, not deleted); `fig:ablations` label removed from the tex, no dangling references. |
| 34 | **Which GNNs are we actually comparing against** | Baselines list SETTLED 2026-07-21; entropy-analysis GNN choice still open | Full inventory: GINEConv (foundational), SiGAT, SNEA, CSG/CSG-GSGNN, GSGNN+SGA (SGA = Signed Graph Augmentation, NeurIPS 2024, applied to GSGNN — this is the "recent" variant), CopulaLSP (ICLR 2026); SE-SGformer run but **fully ignored, not mentioned anywhere** (corrected 2026-07-21 — you said ignore it, an earlier pass mentioned-and-excluded it, which was wrong). Baselines paragraph now names the 6 kept models. **Still open**: which GNN(s) to use for the entropy-degradation results (Result 2, Empirical Confirmation panel C) — you were unsure whether to use GINEConv or an SGA variant. Good news: this isn't a data gap — `scripts/lead4_entropy_heterogeneity.py` already computed the entropy-vs-AUC story for BOTH `GINEConv` and `SiGAT` (SiGAT here is **raw SiGAT, not SGA-augmented** — corrected 2026-08-04; an earlier version of this row wrongly claimed it was the SGA-augmented one via `baselines/SGA/sigat_SGA.py`, confirmed with the user it isn't) — so it's a presentation choice (show one, the other, or both), not new work. **Convention going forward: unqualified "SiGAT" always means raw SiGAT everywhere in this codebase/paper unless explicitly marked otherwise.** Related Work's own GNN citation list is now decoupled from this baselines inventory by design (see #8) — you're sourcing Related Work's literature independently. |
| 35 | **Full citation inventory + command reference** | BLOCKED-CITATIONS (list is complete, bibkeys aren't) | Now a standalone file, not "see chat": **`aaai2027/CITATION_SEARCH_GUIDE.md`** (written 2026-07-22, since the earlier chat-only version of this list was lost to compaction once already). Contains the natbib command cheat-sheet (`\citep`/`\citet`/etc.) and every citation spot in the current draft with concrete candidate papers + confidence level per slot, including the gaps found this round that weren't on the checklist before: the per-walk Transformer architecture + masked-edge/MLM objective (Methods, Per-walk classifier), the AUC standard-error method (#17, DeLong/Hanley–McNeil), and Fano's inequality + WL-coloring/GNN-expressiveness (Problem Setting, Appendix). Weakest leads flagged explicitly (SNEA, SGA's exact venue, CSG/CSG-GSGNN/CopulaLSP, the walk-based-sequence-classifier slot) rather than guessed. |

## What to spin out as separate agent-session requests

Per the standing rule (same pattern as `LEAD4C_DIRECTIONALITY_EXPERIMENTS_NEEDED.md`), items that
need real analysis/plotting rather than just transcription of numbers already in hand should get
their own request doc once we've agreed on the design:
- ~~#22 (attention-direction analysis)~~ — DONE 2026-08-04, in this session (no spin-out needed, reused already-cached `scripts/attention_directionality.py` outputs).
- ~~#12/#21/#29 figure production~~ — DONE 2026-07-26, in this session.
- ~~#25 (plain-mean-vote ablation)~~ — DONE 2026-07-26, computed fresh, didn't need spinning out.
- ~~#23 (full-vs-local ablation figure)~~ — DONE 2026-07-26, as a sketch (still on hold for a from-scratch redo elsewhere).
- ~~#31 (architecture schematic)~~ — moot, you're building the diagram directly rather than via a spun-out agent prompt.
- **#32 (runtime log-log curve)** — genuinely new experiment (synthetic-graph scaling sweep); needs explicit sign-off before launching, then spin out.
